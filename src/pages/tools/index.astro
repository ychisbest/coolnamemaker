---
import Layout from '../../layouts/Layout.astro';
// 读取datas目录下的所有json文件并合并
const tools = await Astro.glob('./datas/*.json').then(files => 
    files.flatMap(file => file.default)
);

// 分页相关参数
const itemsPerPage = 6;
const currentPage = Astro.url.searchParams.get('page') ? parseInt(Astro.url.searchParams.get('page')) : 1;
const totalPages = Math.ceil(tools.length / itemsPerPage);
const startIndex = (currentPage - 1) * itemsPerPage;
const endIndex = startIndex + itemsPerPage;
const currentTools = tools.slice(startIndex, endIndex);
---

<Layout title="Tools - CoolNameMaker">
        <main class="min-h-screen bg-gradient-to-b">
            <div class="container mx-auto px-4 py-16">
                <h1 class="text-2xl font-bold mb-12 text-center bg-gradient-to-r from-purple-600 via-blue-500 to-purple-600 bg-clip-text text-transparent">
                    Name Generator Tools
                </h1>
                
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                    {currentTools.map(tool => (
                        <a href={`/tools/${tool.slug}`} class="group relative overflow-hidden border-gray-100 border-2 bg-white p-8 rounded-2xl shadow-lg hover:shadow-2xl transform hover:-translate-y-1 transition-all duration-300">
                            <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-purple-500 to-blue-500 transform scale-x-0 group-hover:scale-x-100 transition-transform duration-300"></div>
    
                            <div class="text-4xl mb-4">{tool.icon}</div>
                            <h2 class="text-2xl font-semibold mb-3 text-gray-800 group-hover:text-purple-600 transition-colors">
                                {tool.name}
                            </h2>
                            <p class="text-gray-600">
                                {tool.description}
                            </p>
                            <div class="mt-4 text-purple-600 flex items-center gap-2 opacity-0 group-hover:opacity-100 transition-opacity">
                                <span>Try it</span>
                                <svg class="w-4 h-4 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
                                </svg>
                            </div>
                        </a>
                    ))}
                </div>
    
                {/* Pagination Navigation */}
                <div class="flex justify-center mt-12 gap-2">
                    {/* First Page Button */}
                    {currentPage > 1 && (
                        <a href={`/tools?page=1`} class="px-4 py-2 bg-white text-purple-600 rounded-lg border border-purple-600 hover:bg-purple-600 hover:text-white transition-colors">
                            First
                        </a>
                    )}
    
                    {/* Previous Page Button */}
                    {currentPage > 1 && (
                        <a href={`/tools?page=${currentPage - 1}`} class="px-4 py-2 bg-white text-purple-600 rounded-lg border border-purple-600 hover:bg-purple-600 hover:text-white transition-colors">
                            Previous
                        </a>
                    )}
    
                    {/* Page Numbers */}
                    {currentPage > 3 && (
                        <span class="px-4 py-2 text-gray-500">...</span>
                    )}
                    {Array.from({ length: Math.min(5, totalPages) }, (_, i) => {
                        const page = Math.max(1, currentPage - 2) + i;
                        return (
                            page <= totalPages && (
                                <a key={page} href={`/tools?page=${page}`} class={`px-4 py-2 rounded-lg border transition-colors ${currentPage === page ? 'bg-purple-600 text-white border-purple-600' : 'bg-white text-purple-600 border-purple-600 hover:bg-purple-600 hover:text-white'}`}>
                                    {page}
                                </a>
                            )
                        );
                    })}
                    {currentPage < totalPages - 2 && (
                        <span class="px-4 py-2 text-gray-500">...</span>
                    )}
    
                    {/* Next Page Button */}
                    {currentPage < totalPages && (
                        <a href={`/tools?page=${currentPage + 1}`} class="px-4 py-2 bg-white text-purple-600 rounded-lg border border-purple-600 hover:bg-purple-600 hover:text-white transition-colors">
                            Next
                        </a>
                    )}
    
                    {/* Last Page Button */}
                    {currentPage < totalPages && (
                        <a href={`/tools?page=${totalPages}`} class="px-4 py-2 bg-white text-purple-600 rounded-lg border border-purple-600 hover:bg-purple-600 hover:text-white transition-colors">
                            Last
                        </a>
                    )}
                </div>
            </div>
        </main>
    </Layout>
    